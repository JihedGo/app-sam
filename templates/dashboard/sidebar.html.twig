
<div class="sidebar" id="sidebar">
            <div class="sidebar-inner slimscroll">
                <div id="sidebar-menu" class="sidebar-menu">
                    <ul>
                        <li class="menu-title">Menu</li>
                        {% if app.user.role == "ROLE_ADMIN" %}
                        <li class="active">
                            <a href="{{ path('dashboard') }}"><i class="fa fa-dashboard"></i> <span>Dashboard</span></a>
                        </li>
                        {% endif %}
                        {% if (app.user.role == "ROLE_ADMIN") %}
                            <li>
                                <a href="{{ path('medecin_index') }}"><i class="fa fa-user-md"></i> <span>Médecins</span></a>
                            </li>
                             <li>
                                <a href="{{ path('secretaire_index') }}"><i class="fa fa-calendar"></i> <span>Sécretaires</span></a>
                            </li>
                            <li>
                                <a href="{{ path('patient_index') }}"><i class="fa fa-wheelchair"></i> <span>Patients</span></a>
                            </li>

                            <li>
                                <a href="{{ path('rendez_vous_index') }}"><i class="fa fa-calendar"></i> <span>Rendez-Vous</span></a>
                            </li>

                        {% elseif (app.user.role == "ROLE_SECRETAIRE") %}
                             <li>
                                <a href="{{ path('patient_index') }}"><i class="fa fa-wheelchair"></i> <span>Patients</span></a>
                            </li>
                            <li>
                                <a href="{{ path('rendez_vous_index') }}"><i class="fa fa-calendar"></i> <span>Rendez-Vous</span></a>
                            </li>
                        {% elseif (app.user.role == "ROLE_MEDECIN") %}
                             <li>
                                <a href="{{ path('secretaire_index') }}"><i class="fa fa-calendar"></i> <span>Sécretaires</span></a>
                             </li>
                             <!--<li>
                                <a href="{{ path('patient_index') }}"><i class="fa fa-wheelchair"></i> <span>Patients</span></a>
                             </li>-->
                             <li>
                                <a href="{{ path('rendez_vous_index') }}"><i class="fa fa-calendar"></i> <span>Rendez-Vous</span></a>
                             </li>
                        {% elseif (app.user.role == "ROLE_PATIENT") %}
                             <li>
                                <a href="{{ path('medecin_list') }}"><i class="fa fa-wheelchair"></i> <span>Liste des médecins</span></a>
                            </li>
                            <li>
                                <a href="{{ path('mes_rendezvous') }}"><i class="fa fa-calendar"></i> <span>Mes Rendez-Vous</span></a>
                            </li>
                        {% endif %}

                    </ul>
                </div>
            </div>
</div>
